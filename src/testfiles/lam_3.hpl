    fun (Int () -> Int ()) sil (y) = { x ->
        force let
            () :: () = print ("uuu");
        in if x () == 0 then 1 else x () * sil (y (), x () - 1)
    };
    
    sus fun (() -> Int ()) main () =
        force let
            () :: x = { e -> sil (e ()) };
            () :: () = print (x (1, 1));
            //() :: y = { e -> x (e ()) };
            //(Int ()) :: z = y (3);
            //() :: () = print (z ());
            //() :: () = print (sil (4));
            //() :: () = print (sil (4, 5));
            //() :: () = print (x (1));
            //() :: () = print (y (1));
        in 0;