struct Main {
    fun () e () = e ();
    fun (Int () -> Int ()) sil (x, y) = if x () == 0 then 1 else x () * sil (x () - 1, y ());

    fun (() -> Int ()) main () =
        force let
            () :: () = print (sil (3, e ()));
        in 0;
};
