struct Main {
    fun (Int () -> Int ()) sil (x) = if x () == 0 then 1 else x () * sil (x () - 1);


    fun (() -> Int ()) main () =
        force let
            () :: () = print (sil (4));
        in 0;
};
