struct Main {
    fun (Int () -> Int ()) another_two (x) = x () * 2;

    fun (() -> Int ()) main () =
        force let
            (Int () -> Int ()) :: x = another_two ();
            () :: () = print (x (2));
        in 0;
};
