struct Main {
    fun (Int () -> (Int () -> (Int () -> Int ()))) another_two (a, b, x) = x () * 2;

    fun (() -> Int ()) main () =
        force let
            (Int () -> Int ()) :: x = another_two (0, 1);
            () :: () = print (x (2));
        in 0;
};
