The Language Hpl
BNF Converter


%This txt2tags file is machine-generated by the BNF-converter
%Process by txt2tags to generate html or latex



This document was automatically generated by the //BNF-Converter//. It was generated together with the lexer, the parser, and the abstract syntax module, which guarantees that the document matches with the implementation of the language (provided no hand-hacking has taken place).

==The lexical structure of Hpl==
===Identifiers===
Identifiers //Ident// are unquoted strings beginning with a letter,
followed by any combination of letters, digits, and the characters ``_ '``
reserved words excluded.


===Literals===
Integer literals //Integer// are nonempty sequences of digits.


String literals //String// have the form
``"``//x//``"``}, where //x// is any sequence of any characters
except ``"`` unless preceded by ``\``.




===Reserved words and symbols===
The set of reserved words is the set of terminals appearing in the grammar. Those reserved words that consist of non-letter characters are called symbols, and they are treated in a different way from those that are similar to identifiers. The lexer follows rules familiar from languages like Haskell, C, and Java, including longest match and spacing conventions.

The reserved words used in Hpl are the following:
  | ``data`` | ``else`` | ``force`` | ``fun``
  | ``if`` | ``in`` | ``let`` | ``private``
  | ``ref`` | ``struct`` | ``sus`` | ``then``

The symbols used in Hpl are the following:
  | { | } | ; | (
  | ) | = | , | ->
  | | | :: | . | [
  | ] | + | - | %
  | * | / | < | <=
  | > | >= | == | !=

===Comments===
Single-line comments begin with //.Multiple-line comments are  enclosed with /* and */.

==The syntactic structure of Hpl==
Non-terminals are enclosed between < and >. 
The symbols -> (production),  **|**  (union) 
and **eps** (empty rule) belong to the BNF notation. 
All other symbols are terminals.

  | //Program// | -> | //[StructOrInterfaceOrType]// 
  | //StructOrInterfaceOrType// | -> | //Struct// 
  |  |  **|**  | //AlgType// 
  | //[StructOrInterfaceOrType]// | -> | **eps**
  |  |  **|**  | //StructOrInterfaceOrType// //[StructOrInterfaceOrType]// 
  | //Struct// | -> | ``struct`` //Ident// ``{`` //StructBody// ``}`` ``;`` 
  | //StructBody// | -> | //[StructField]// 
  | //StructField// | -> | ``private`` //FunctionDef// 
  |  |  **|**  | //FunctionDef// 
  |  |  **|**  | ``private`` //RefDef// 
  |  |  **|**  | //RefDef// 
  | //[StructField]// | -> | **eps**
  |  |  **|**  | //StructField// //[StructField]// 
  | //FunctionDef// | -> | ``fun`` //Type// //Ident// ``(`` //[FunctionArg]// ``)`` ``=`` //ValueStatement// ``;`` 
  |  |  **|**  | ``sus`` //FunctionDef// 
  | //FunctionArg// | -> | //PatternMatch// 
  | //[FunctionArg]// | -> | **eps**
  |  |  **|**  | //FunctionArg// 
  |  |  **|**  | //FunctionArg// ``,`` //[FunctionArg]// 
  | //ValueStatement// | -> | ``let`` //[Assignment]// ``in`` //ValueStatement// 
  |  |  **|**  | ``force`` ``let`` //[Assignment]// ``in`` //ValueStatement// 
  |  |  **|**  | ``if`` //ValueStatement// ``then`` //ValueStatement// ``else`` //ValueStatement// 
  |  |  **|**  | //ListValueStatementr// 
  |  |  **|**  | //TupleValueStatementr// 
  |  |  **|**  | //FunApplication// 
  |  |  **|**  | //Integer// 
  |  |  **|**  | //String// 
  |  |  **|**  | ``{`` //Ident// ``->`` //ValueStatement// ``}`` 
  |  |  **|**  | //ValueStatement// //ValueStatementExpr// 
  | //[ValueStatement]// | -> | **eps**
  |  |  **|**  | //ValueStatement// 
  |  |  **|**  | //ValueStatement// ``,`` //[ValueStatement]// 
  | //RefDef// | -> | ``ref`` //Type// //Ident// ``=`` //ValueStatement// ``;`` 
  | //Type// | -> | //Ident// ``(`` //[Type]// ``)`` 
  |  |  **|**  | ``(`` //Type// ``->`` //Type// ``)`` 
  |  |  **|**  | ``(`` //[Type]// ``)`` 
  | //[Type]// | -> | **eps**
  |  |  **|**  | //Type// 
  |  |  **|**  | //Type// ``,`` //[Type]// 
  | //AlgType// | -> | ``data`` //Ident// ``(`` //[TypeArg]// ``)`` ``=`` //[AlgTypeVal]// ``;`` 
  | //TypeArg// | -> | //Ident// 
  | //[TypeArg]// | -> | **eps**
  |  |  **|**  | //TypeArg// 
  |  |  **|**  | //TypeArg// ``,`` //[TypeArg]// 
  | //AlgTypeVal// | -> | //Ident// ``(`` //Type// ``)`` 
  | //[AlgTypeVal]// | -> | **eps**
  |  |  **|**  | //AlgTypeVal// 
  |  |  **|**  | //AlgTypeVal// ``|`` //[AlgTypeVal]// 
  | //PatternMatch// | -> | //Integer// 
  |  |  **|**  | //Ident// 
  |  |  **|**  | ``(`` //[PatternMatch]// ``)`` 
  |  |  **|**  | //PatternMatch// ``(`` //[PatternMatch]// ``)`` 
  | //[PatternMatch]// | -> | **eps**
  |  |  **|**  | //PatternMatch// 
  |  |  **|**  | //PatternMatch// ``,`` //[PatternMatch]// 
  | //Assignment// | -> | //Type// ``::`` //PatternMatch// ``=`` //ValueStatement// ``;`` 
  |  |  **|**  | //RefDef// 
  | //[Assignment]// | -> | **eps**
  |  |  **|**  | //Assignment// //[Assignment]// 
  | //FunApplication// | -> | //Ident// ``.`` //FunApplication// 
  |  |  **|**  | //Ident// ``(`` //[FunctionArgAppl]// ``)`` 
  | //FunctionArgAppl// | -> | //ValueStatement// 
  | //[FunctionArgAppl]// | -> | **eps**
  |  |  **|**  | //FunctionArgAppl// 
  |  |  **|**  | //FunctionArgAppl// ``,`` //[FunctionArgAppl]// 
  | //ListValueStatementr// | -> | ``[`` //[ValueStatement]// ``]`` 
  | //TupleValueStatementr// | -> | ``(`` //[ValueStatement]// ``)`` 
  | //ValueStatementExpr// | -> | ``+`` //ValueStatement// 
  |  |  **|**  | ``-`` //ValueStatement// 
  |  |  **|**  | ``%`` //ValueStatement// 
  |  |  **|**  | ``*`` //ValueStatement// 
  |  |  **|**  | ``/`` //ValueStatement// 
  |  |  **|**  | ``<`` //ValueStatement// 
  |  |  **|**  | ``<=`` //ValueStatement// 
  |  |  **|**  | ``>`` //ValueStatement// 
  |  |  **|**  | ``>=`` //ValueStatement// 
  |  |  **|**  | ``==`` //ValueStatement// 
  |  |  **|**  | ``!=`` //ValueStatement// 


